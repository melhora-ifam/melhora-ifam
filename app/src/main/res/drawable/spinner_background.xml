<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cor de fundo com o selector -->
    <solid android:color="@drawable/spinner_background_selector" />

    <!-- Borda -->
    <stroke
        android:width="1dp"
        android:color="#CCCCCC" />

    <!-- Cantos arredondados -->
    <corners android:radius="8dp" />

    <!-- Padding interno -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>